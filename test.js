const { WorkspaceManager } = require('./apps/desktop/src/main/services/workspace-manager.ts');
// Wait, I can't just require a TS file directly without ts-node or vite.
// Let's just create a test in TS.
